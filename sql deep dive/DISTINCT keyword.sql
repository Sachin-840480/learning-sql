-- Distinct Keyword

SELECT DISTINCT salary FROM salaries;

-- select DISTINCT salary, from_date from salaries;     -- for multiple rows

-- we got more values in return from above query because, very few people have the same salary and from_date, i.e less duplicates are present, so more data is returned.